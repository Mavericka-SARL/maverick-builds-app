# Schema Migration service — gRPC on port 9090.
# Applies and tracks database schema migrations (goose or similar).
# In many setups this runs as a one-shot Job; here it also exposes a gRPC
# endpoint so other services can trigger/query migration status.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: schema-migration
  namespace: mavericks
  labels:
    app: schema-migration
    app.kubernetes.io/name: schema-migration
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: mavericks-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: schema-migration
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: schema-migration
        app.kubernetes.io/name: schema-migration
        app.kubernetes.io/component: service
        app.kubernetes.io/part-of: mavericks-engine
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: schema-migration
          image: registry.example.com/mavericks/schema-migration:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mavericks-config
            - secretRef:
                name: mavericks-secrets
          # DATABASE_URL is composed here, not in the ConfigMap: envFrom
          # never expands $(VAR) references — only an explicit env: entry
          # can, by referencing $(POSTGRES_PASSWORD) already contributed
          # above via envFrom's secretRef.
          env:
            - name: DATABASE_URL
              value: "postgres://mavericks:$(POSTGRES_PASSWORD)@pgbouncer:5433/mavericks?sslmode=disable"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          readinessProbe:
            exec:
              command:
                - grpc-health-probe
                - -addr=:9090
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            exec:
              command:
                - grpc-health-probe
                - -addr=:9090
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities:
              drop: ["ALL"]
      securityContext:
        fsGroup: 65534

---
apiVersion: v1
kind: Service
metadata:
  name: schema-migration
  namespace: mavericks
  labels:
    app: schema-migration
    app.kubernetes.io/name: schema-migration
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: mavericks-engine
spec:
  type: ClusterIP
  selector:
    app: schema-migration
  ports:
    - name: grpc
      port: 9090
      targetPort: 9090
      protocol: TCP
